module ReexportFacade

// One import brings the whole library in, so a consumer imports the facade
// instead of every file behind it.

import { "./reexport_origin.mmt" }

public export { Point, Shade, Double, Identity }
