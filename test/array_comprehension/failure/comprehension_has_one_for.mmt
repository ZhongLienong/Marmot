module ComprehensionHasOneFor

// A second `for` reported its variable as an undefined name at the element, where it is
// used, rather than saying a comprehension takes one `for`.

def pairs = [(x, y) for x in [1, 2] for y in [3, 4]];
